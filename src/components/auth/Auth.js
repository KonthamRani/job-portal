import React from 'react'
// import { useParams } from 'react-router-dom'

function Auth({type}) {
    // const param=useParams();
    // const id=param.id
  return (
    <div>
      Auth for {type} 
      {/* and id is {id} */}
    </div>
  )
}

export default Auth
